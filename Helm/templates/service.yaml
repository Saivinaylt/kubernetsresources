kind: Service
apiVersion: v1
metadata:
  name: helpservice
  labels:
    service: loadbalancer
spec:
  type: LoadBalancer
  selector:
    resource: helmchart
  ports:
  - name: http-port
    protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: 80
